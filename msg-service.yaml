apiVersion: v1
kind: Service
metadata:
  name: msg-service
  labels:
    app: msg-service
spec:
  type: NodePort
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30048
  selector:
    app: msg-service